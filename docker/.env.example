# Development Ports Configuration
# Copy this file to .env and adjust ports as needed

# HTTP Port (default: 80 in container)
PORT_HTTP=4070

# Admin UI Port (default: 81 in container)
PORT_ADMIN=4071

# HTTPS Port (default: 443 in container)
PORT_HTTPS=4072

# Vite Dev Server Port (default: 5173 in container)
PORT_VITE=4073

# Database Configuration (optional)
# Uncomment and configure if using MySQL/PostgreSQL instead of SQLite

# MySQL Configuration
# DB_MYSQL_HOST=db
# DB_MYSQL_PORT=3306
# DB_MYSQL_USER=npm
# DB_MYSQL_PASSWORD=npm
# DB_MYSQL_NAME=npm

# PostgreSQL Configuration (default in dev)
DB_POSTGRES_HOST=db-postgres
DB_POSTGRES_PORT=5432
DB_POSTGRES_USER=npm
DB_POSTGRES_PASSWORD=npmpass
DB_POSTGRES_NAME=npm

# SQLite Configuration
# DB_SQLITE_FILE=/data/database.sqlite